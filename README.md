Advent of Code 2025 Solutions (in Golang)

To run, save puzzle inputs in a top level folder called 'inputs', and name them 'day{number}.txt'.

Then, simply run 'go run ./cmd/aoc/main.go -day {number} -part {1/2}'